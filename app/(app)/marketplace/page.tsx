import { ProductBrowser } from "@/components/marketplace/ProductBrowser";

export default function MarketplacePage() {
  return (
    <ProductBrowser
      title="Marketplace"
      description="Browse plugins, resources, builds, and configurations from the Visual Studio community."
    />
  );
}
